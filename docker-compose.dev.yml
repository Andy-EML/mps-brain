services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: mps
      POSTGRES_PASSWORD: mps
      POSTGRES_DB: mps
    ports: ['5432:5432']
    volumes: ['pgdata-dev:/var/lib/postgresql/data']
volumes:
  pgdata-dev:
